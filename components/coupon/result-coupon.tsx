export default function ResultCoupon() {
    return (
        <div className="bg-white w-full h-[200px] animate-jump-in animate-once animate-duration-1000 animate-delay-500 animate-normal animate-fill-forwards"></div>
    );

    //     <div
    //     className="transition-transform duration-300 ease-in-out hover:scale-105"
    //     onMouseEnter={() => setIsHover(true)}
    //     onMouseLeave={() => setIsHover(false)}
    // >
    //     {isHover ? (
    //         <Image src={ImgPreopenCoupon} alt="CouponBox" objectFit="cover" />
    //     ) : (
    //         <Image src={ImgCoupon} alt="CouponBox" objectFit="cover" />
    //     )}
    // </div>
}
